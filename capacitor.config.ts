import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ntag424scanner.app',
  appName: 'NTAG 424 TT Scanner',
  webDir: 'dist',
  android: {
    backgroundColor: '#0f172a',
  },
};

export default config;
